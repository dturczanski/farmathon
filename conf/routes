# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index

#Employee authentication
GET     /login                      controllers.Auth.login
POST     /authenticate                      controllers.Auth.authenticate


# Enquiries
GET     /enquiries                  controllers.Enquiries.index
GET     /enquiries/:id              controllers.Enquiries.details(id: Long)
POST    /enquiries/accept/:id       controllers.Enquiries.details(id: Long)
POST    /enquiries/decline/:id      controllers.Enquiries.details(id: Long)


# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
